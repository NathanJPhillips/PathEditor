<Window x:Class="NobleTech.Products.PathEditor.ApplyStyleDialog"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converter="clr-namespace:NobleTech.Products.PathEditor.Converters"
        xmlns:behaviors="clr-namespace:NobleTech.Products.PathEditor.Behaviors"
        xmlns:viewmodels="clr-namespace:NobleTech.Products.PathEditor.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewmodels:ApplyStyleViewModel}"
        Title="Apply Style"
        Width="300" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" WindowStyle="ToolWindow">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5,0,0,0" />
            <Setter Property="Padding" Value="10,5" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Width" Value="150" />
        </Style>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="0,0,5,0" />
        </Style>

        <converter:IsNullOrEmptyConverter IsNegated="True" x:Key="IsNotNullConverter" />
    </Window.Resources>
    <StackPanel Margin="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Target="{Binding ElementName=StyleComboBox}">_Style:</Label>
            <ComboBox x:Name="StyleComboBox"
                      Grid.Row="0" Grid.Column="1"
                      ItemsSource="{Binding Styles}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedStyle}"
                      behaviors:Focus.IsFocusedOnLoad="True" />

            <Label Grid.Row="1">Apply:</Label>
            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,5,0,0">
                <CheckBox IsEnabled="{Binding SelectedStyle.StrokeColor, Converter={StaticResource IsNotNullConverter}}" IsChecked="{Binding UseStrokeColor}" Margin="0,0,0,10">_Colour</CheckBox>
                <CheckBox IsEnabled="{Binding SelectedStyle.StrokeThickness, Converter={StaticResource IsNotNullConverter}}" IsChecked="{Binding UseStrokeThickness}" Margin="0,0,0,10">_Thickness</CheckBox>
            </StackPanel>
        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Command="{Binding ApplyCommand}">_Apply</Button>
            <Button IsDefault="True" Command="{Binding OKCommand}">OK</Button>
            <Button IsCancel="True">Cancel</Button>
        </StackPanel>
    </StackPanel>
</Window>
